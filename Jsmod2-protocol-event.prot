!jsmod2 protocol about event (use for jsmod2 protocol 4.5)

inline include do.prot

define {eventId} as id

start:
  event admin
  adminquery setAdmin
      id 0x66
      type event
      admin {value}
      player {eventId}
done

